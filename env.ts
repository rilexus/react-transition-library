const basename = "/react-transitions-library";

const production = process.env.production;
const development = process.env.development;
const repoURL = "https://github.com/rilexus/react-transitions-library";
const repoTransitionsURL = `${repoURL}/blob/main/transitions`;

export { basename, production, development, repoTransitionsURL, repoURL };
